name: Build
on:
  push:
    branches:
      - 'develop'
jobs:
  Build-Image:
    runs-on: ubuntu-latest
    steps:
      - name: Checking out this repository
        uses: actions/checkout@v2
      - run: docker ps
